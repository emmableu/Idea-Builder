const globalConfig = {
     spriteDrawerWidth: 230,
     storyboardDrawerWidth: 210,
     toolBarHeight: 48,
     boardContentPadding: 8,
     addNewSpriteBoxHeight: 50,
}

Object.freeze(globalConfig);

export default globalConfig;
